name: zekra
module: github.com/fadymondy/zekra

database:
  driver: pgx
  url: ${DATABASE_URL}

api:
  graphql: /graphql
  rest: /api
  docs: /docs

auth:
  provider: supabase

frontend:
  dir: web

plugins: []
