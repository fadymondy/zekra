// Zekra's cube mark comes from the grid's product marks (synced from fadymondy.com-v2; the
// entry is still keyed `cabrain` upstream).
export type { Cell, MarkSpec } from "@/app/styles/grid-marks"
import { PRODUCT_MARKS, type MarkSpec } from "@/app/styles/grid-marks"

export const ZEKRA_MARK: MarkSpec = PRODUCT_MARKS.cabrain
