import { NotificationCenter } from "@/features/notify";

// The notification center (MH-360): the signed-in account's inbox.
export default function NotificationsScreen() {
  return <NotificationCenter />;
}
